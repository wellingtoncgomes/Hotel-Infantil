<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <ul>
        <% reservas.forEach(p => { %>
            <li><%= p.data_reserva %> <%= p.nome_crianca %> <%= p.status_reserva %></li>
        <% }); %>
    </ul>
</body>
</html>