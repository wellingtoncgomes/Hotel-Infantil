<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Criar usuário</title>
</head>
<body>
  <h1>Create New User</h1>
  <form action="/criarUsuario" method="POST">
    <label for="email">Email:</label>
    <input type="text" id="email" name="email" value="<%= usuario ? usuario.usuario : '' %>"" required>
    <br>
    <label for="password">Senha:</label>
    <input type="password" id="password" name="password" value="<%= usuario ? usuario.password : '' %>" required>
    <br>
    <button type="submit">Criar usuário</button>
  </form>

  <section>
    <% if(errors) { %>
        <ul>
            <% for(let i = 0; i < errors.length; i++){ %>
                <li>
                    <%= errors[i].message %>
                </li>
            <% } %>
        </ul>
    <% } %>
</section>
</body>
</html>
